# NetworkManager gère les interfaces, systemd-resolved gère la résolution DNS
# (stub resolver déjà utilisé par /etc/resolv.conf -> /run/systemd/resolve/stub-resolv.conf).
[main]
dns=systemd-resolved
